spring:
  application:
    name: course-service

  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true

  data:
    mongodb:
      uri: mongodb://localhost:27017/coursedb

#  cloud:
#    netflix:
#      eureka:
#        client:
#          service-url:
#            defaultZone: http://localhost:8761/eureka

server:
  port: 8070